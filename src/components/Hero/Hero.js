const Hero = () => {
    return (
        <div>
            <h1>My first React App</h1>
            <p>Simple to-do app, with lists, columns and card</p>
        </div>
    );
};

export default Hero;